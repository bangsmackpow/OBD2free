{
  "name": "obd2free-api",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build:worker": "wrangler build",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute obd2free-db --file=./migrations/001_initial_schema.sql --remote",
    "db:local": "wrangler d1 execute obd2free-db --file=./migrations/001_initial_schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240418.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.1.3",
    "wrangler": "^4.86.0"
  }
}
